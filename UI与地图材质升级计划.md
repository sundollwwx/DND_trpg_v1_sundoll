# 主控台 UI 与地图材质升级计划

## 当前进度（2026-09-01）

- 已完成 M1 的第一轮：四个工具分区、可隐藏左栏、按选择展开的单位属性栏，以及地图右下悬浮快捷工具。
- 已完成 M3 与 M4 的首轮资源包：石地板、木地板、草地、土路、水面、石墙各有两张 512×512 的 AI 静态纹理；地图工坊和主控台共用 `地图/tile-renderer.js`。
- 已完成 M5 的基础自动连接与确定性变体：墙边、水岸和草土交界由 Canvas 绘制；每格根据坐标稳定选择纹理变体与裁切位置，不增加地图 JSON 字段，也不会产生额外联机同步。
- M6 已完成核心编辑体验：真实材质缩略图、搜索、收藏、最近使用、1×1/2×2/3×3/5×5 笔刷、画笔拖拽、Shift 直线、直线/矩形/填充、选区、撤销与重做。局部变体刷新和图层系统留作后续扩展。
- M7 已开放 100×100 地图尺寸并完成页面回归；大地图的首次绘制、拖动、缩放、缓存压力和旧档兼容性仍需做有量化指标的专项验证。
- M8 已完成第一轮：底部流程条支持“先攻 / 世界时间”切换、自由/回合制、年周日历、短休长休、旧先攻存档迁移，以及通过 SSE 同步到观战台；浏览器页面和跨日/跨年边界已回归通过。
- M9 已完成第一轮：回合制下由服务器锁定玩家移动权限，并记录、同步和在换回合时清除本回合移动路径；已完成服务端伪造/过期请求、骑乘控制组、观战台拖动回滚和页面级联调。玩家身份仍是观战台填写的名字，属于朋友间使用的信任模型。

## 一、改造目标

- 让地图成为主控台的视觉中心，减少顶栏和侧栏对地图空间的挤压。
- 保留“暗色 GM 控制台 + 暖金强调色”的现有风格，同时提高信息层级与操作效率。
- 将地图材质从简单纯色和线条升级为“AI 生成材质美术 + Canvas 结构绘制”的混合方案。
- 让地图工坊和主控台共用同一套地块渲染逻辑，避免两边显示效果漂移。
- 保持旧地图存档格式兼容，并确保材质升级不破坏当前的轻量联机同步。

## M0：视觉规范与改造基线

- 保存主控台和地图工坊当前界面截图，建立改造前后对照基线。
- 统一背景、面板、悬浮层、强调色、字号、间距、圆角和阴影规则。
- 规定按钮、输入框、标签页、工具栏、弹窗和状态提示的视觉与交互状态。
- 主操作逐步采用统一线性图标；有助于识别的 emoji 可以保留。
- 本阶段不改变功能，只确定信息架构和视觉语言。

## M1：主控台信息架构重排

核心原则：地图最大、常用操作一步可达。

- 顶栏只保留战役/地图名称、联机状态、保存状态、适应窗口和“更多”菜单。
- 左侧工具区改为“单位、地图、绘制、资源”四个标签页。
- 右侧属性栏只在选中棋子时展开；未选中时自动收起。
- 网格、吸附、名字、迷雾、缩放等操作移入地图右下角悬浮工具条。
- 骰子改为可收起浮层或快捷栏。
- BGM 改为底部迷你播放器。
- 增加一键隐藏侧栏、侧栏宽度调整和小屏布局。

## M2：统一 UI 组件

- 建立统一设计令牌：颜色、字体、间距、圆角、阴影和层级。
- 统一按钮、图标按钮、输入框、选择框、卡片、标签页和工具栏。
- 统一 Tooltip、Modal、Toast、联机状态和保存状态组件。
- 完善 hover、active、disabled、focus 和当前工具高亮状态。
- 减少边框套边框的视觉噪声，更多使用背景层次、留白和轻阴影区分区域。

## M3：AI 材质资源与共享 Canvas 渲染引擎

材质采用明确的三层结构：

1. AI 美术层：离线生成可无缝平铺的地面、墙体和覆盖层素材。
2. Canvas 结构层：负责格子布局、材质铺设、边缘连接、墙角、阴影、网格、迷雾和交互状态。
3. 地图数据层：只记录材质 ID、格子坐标、变体编号和随机种子，不保存图片或 Base64。

具体工作：

- 为每种常用材质生成 3–6 个统一风格的无缝纹理变体，而不是在运行时为每个格子单独调用 AI。
- AI 素材统一要求：正俯视、无透视、无文字、无独立物件、光照方向一致、色调统一、四边可无缝衔接。
- 素材生成后进行接缝检查、色调统一、裁切、压缩和正常尺寸预览，合格后才进入正式材质库。
- 基础地面建议使用 256×256 或 512×512 的 WebP/PNG；需要透明度的边缘和覆盖层使用透明 PNG/WebP。
- 抽出共享地块渲染模块，例如 `地图/tile-renderer.js`，供地图工坊和主控台共同调用。
- Canvas 使用“格子坐标 + 材质 ID + 地图种子”确定变体、偏移和允许的旋转，保证重绘结果稳定。
- 方向敏感的木板、墙砖和水流不随意旋转，由材质配置控制可用变换。
- 对 AI 纹理、边缘蒙版和最终 tile 使用浏览器静态资源缓存及离屏画布缓存，避免每帧重复解码和生成。

## M4：第一批高价值材质

- 石地板：AI 提供石块、裂纹、缺角和污渍质感；Canvas 负责格子边界和墙体投影。
- 木地板：AI 提供木纹、钉子和磨损；Canvas 控制木板方向、板缝连续性和房间边缘。
- 草地：AI 提供多层绿色、草叶簇和裸土斑；Canvas 负责草地与泥土的过渡蒙版。
- 泥土：AI 提供细颗粒、小石块和浅色干裂；车辙、脚印等作为独立覆盖层。
- 水面：AI 提供水色、波纹和细微反光；Canvas 负责水岸形状、深浅区域和岸边泡沫。
- 石墙：AI 提供砖石、苔藓和破损质感；Canvas 负责直线、转角、尽头、顶面、侧面和内侧投影。

第一轮先建立一套风格统一的小型 AI 材质包，并控制图片尺寸和压缩质量。朋友打开网页时只加载静态材质资源，浏览器缓存后不再重复下载，也不需要安装任何 AI 工具。

## M5：自动连接、变体与覆盖层

- 根据相邻格子自动判断墙体、水岸、草土过渡和道路边缘。
- 使用 bitmask/autotile 生成直线、转角、尽头、交叉和边角连接。
- Canvas 从每种材质的 3–6 个 AI 变体中确定性选取，并使用受控偏移或镜像减少棋盘式重复感。
- 增加独立覆盖层：苔藓、潮湿、烧焦、血迹、灰尘、污渍、碎石和落叶。
- 覆盖层不创建新的基础地块类型，避免地块列表膨胀。
- 桌椅、树木、岩石等独立物件可使用 AI 生成的透明背景素材，但仍由 Canvas 负责位置、层级、缩放和碰撞区域。

## M6：地图工坊编辑体验

- 材质面板使用可视缩略图，并按地面、墙体、物件、覆盖层分类。
- 增加最近使用、收藏和搜索。
- 增加 1×1、3×3、5×5 笔刷。
- 支持 Shift 直线、矩形房间和区域填充。
- 提供材质参数预览和局部随机变体刷新。
- 明确撤销、重做和当前图层状态。

## M7：兼容性与性能验证

- 旧地图 JSON 必须可以直接读取，不强制迁移数据格式。
- 地图工坊与主控台必须使用同一渲染器，并保证显示一致。
- 测试 20×20、50×50、100×100 地图的首次绘制、缩放、拖动和重绘性能。
- 测试首次加载、浏览器缓存命中和离线局域网使用时的素材加载表现。
- 验证 AI 材质和 Canvas tile 缓存不会造成明显内存增长或长时间卡顿。
- 联机继续只同步地图状态与小动作包，不同步实时纹理或高频 Base64 数据。
- 材质图片作为服务器静态资源按需加载，并使用哈希或版本号更新缓存。
- 玩家只通过浏览器加入，不需要安装额外工具。

## M8：先攻、世界时间与游戏模式同步系统

### M8.1 交互结构

把现有地图下方的“先攻与时间条”升级为统一的游戏流程条：

- 默认保持折叠，避免压缩地图；折叠状态只显示当前模式、当前日期和回合摘要。
- 展开后提供“先攻”和“时间”两个标签页，同一时刻只显示一组功能，切换标签不会重置另一页的数据。
- 顶部始终保留“自由模式 / 回合制”切换按钮，使主持人不必进入某个标签页才能改变游戏节奏。
- “先攻”页负责先攻列表、当前行动单位、轮次和推进操作；“时间”页负责世界日期、时刻、运行状态、短休和长休。
- 小屏幕下先保证模式切换、当前日期、当前单位和“下一位”可见，较长的先攻列表改为横向滚动，不让底栏无限增高。

### M8.2 自由模式与回合制语义

- 旧存档和新战役默认进入“自由模式”。
- 自由模式下仍可编辑、排序和查看先攻列表，但不高亮或锁定当前行动单位，“下一位”按钮禁用，轮次不自动推进。
- 回合制下高亮当前行动单位，启用“下一位”；从列表末尾回到第一位时，轮次自动加一。
- 两种模式切换时保留先攻列表、当前单位和轮次。切回回合制后从原位置继续，避免误操作导致战斗进度丢失。
- M8 第一版模式切换只管理流程显示与推进按钮；严格玩家行动锁定由 M9 接管，自由模式继续保持原有移动权限。
- 主持人切换模式后，主控台和观战台同时显示清晰的模式徽标。

### M8.3 先攻页

- 保留手动输入“单位名称 + 先攻值”的方式，并按先攻值降序稳定排列。
- 增加从当前地图棋子一键加入先攻的入口；条目记录对应棋子 ID，棋子改名后可同步显示，重复加入时给出提示。
- 当前行动单位使用稳定的条目 ID 记录，而不是只记录数组下标，避免删除或重新排序后指向错误单位。
- 回合制下支持点击条目指定当前行动单位，并提供“下一位”和“重置战斗”操作；重置战斗需要明确区分“只重置轮次”与“清空列表”。
- 观战台显示只读先攻条，包含模式、第几轮、当前行动单位和完整顺序；玩家端不能修改顺序或推进回合。

### M8.4 世界时间与休息规则

第一版采用固定、明确的游戏世界历法：

- 每天 24 小时，每周 7 天，每年 52 周。
- 默认起点为“第 1 年 · 第 1 周 · 第 1 天 08:00”。
- 主显示固定包含“第几年 / 第几周 / 第几天”，并同时显示 24 小时时刻。
- 主持人可以直接修改年、周、日和时刻，也可以开始或暂停世界时间。
- 短休使世界时间前进 1 小时，长休使世界时间前进 8 小时；跨天、跨周、跨年时自动正确进位。
- 执行休息时先结算正在运行的世界时间，再追加休息时长，避免丢失已经经过的时间。
- 第一版长休和短休只推进时间并留下最近事件提示，不自动恢复棋子的 HP，也不引入额外规则系统。

世界时间使用一个规范化的总分钟数作为权威值，年、周、日和时刻都由它换算，避免多个字段分别进位造成日期不一致：

```js
encounter: {
  collapsed: true,
  panel: 'initiative',
  playMode: 'free',
  round: 1,
  currentEntryId: null,
  entries: [],
  worldTime: {
    totalMinutes: 480,
    runningSince: null,
    rate: 1
  },
  lastEvent: null
}
```

其中 `rate: 1` 表示世界时间运行时，每经过 1 个现实分钟推进 1 个游戏分钟。后续如果确有需要，可以再增加时间倍率选项，但不放进第一轮界面。

### M8.5 主控台与观战台同步

- 主控台是先攻、游戏模式和世界时间的唯一权威来源；观战台只负责显示。
- `buildStreamPayload()` 明确加入经过精简的 `encounter` 公共状态，不能依赖完整本地存档被偶然带入。
- 开始、暂停、短休、长休、切换模式、修改先攻、指定当前单位或推进回合时，立即标记联机状态需要推送。
- 世界时间运行时不每秒上传完整状态。主控台只发送基础总分钟数、开始时间和速率，观战台在本地更新显示。
- 服务器在 SSE 状态消息中附带服务器当前时间，观战台据此校准运行中的世界时间，避免朋友电脑系统时钟不同导致日期漂移。
- 观战台重连或刷新后，应从服务器保存的最新状态恢复先攻顺序、模式、轮次、当前单位和世界时间。
- 折叠状态和主控台当前打开的标签页属于主持人本地界面偏好，不要求强制同步；实际模式、日期和先攻状态必须同步。
- 同步仍沿用当前 `/api/state` + `/api/events` 的轻量 SSE 结构，不增加每帧、每秒或随 Canvas 重绘发送的数据。

### M8.6 旧存档迁移与异常保护

- 旧存档没有 `encounter` 时自动补齐默认结构，不要求用户手工迁移。
- 已有第一版先攻条的存档保留 `entries`、`round`、`current` 和 `collapsed`；首次读取时把下标式 `current` 转为 `currentEntryId`。
- 旧的 `elapsedMs` 分钟计时不自动解释成世界日期，避免把一段战斗计时错误变成数年的世界时间；世界时间从默认日期初始化。
- 对缺失、负数、非法数字和越界日期做统一归一化；空先攻列表时当前单位必须为 `null`，轮次最低为 1。
- 删除当前行动单位后，回合制自动选择合理的下一项；删除最后一项后回到空列表状态，不产生数组越界。

### M8.7 实施顺序

1. 重构 `主控台/app.js` 中的 `encounter` 默认值、归一化逻辑和旧存档迁移。
2. 改造 `主控台/主控台.html` 与 `主控台/style.css`，完成双标签页、世界日期、休息按钮和模式切换布局。
3. 完成先攻列表与地图棋子的关联、稳定当前条目、自由/回合制行为和休息进位逻辑。
4. 扩展 `buildStreamPayload()` 和 `主控台/联机服务器.py` 的时间校准字段，确保状态按事件推送而非高频推送。
5. 在 `主控台/观战.html` 增加只读流程条，并处理刷新、断线重连和本地时钟刷新。
6. 清理第一版先攻分钟计时的旧事件与死代码，再进行主控台、观战台和旧存档联合回归。

### M8.8 验收标准

- 底栏默认折叠；展开后“先攻 / 时间”可即时切换，数据互不丢失。
- 自由模式下不能误推进轮次；回合制下从最后一项点击“下一位”会回到第一项并将轮次加一。
- “第 1 年 · 第 1 周 · 第 1 天 23:00”短休后正确变为“第 1 年 · 第 1 周 · 第 2 天 00:00”。
- “第 1 年 · 第 52 周 · 第 7 天 20:00”长休后正确变为“第 2 年 · 第 1 周 · 第 1 天 04:00”。
- 主控台修改模式、先攻、当前单位或世界时间后，观战台在一次状态推送内更新；玩家端无法反向修改这些权威状态。
- 世界时间连续运行 10 分钟期间不产生逐秒完整状态上传，主控台和至少两个观战页面的显示保持一致。
- 刷新观战页、SSE 自动重连和主控台重新载入存档后，模式、轮次、当前单位和世界时间都能恢复。
- 旧存档可直接打开，原有先攻条目不丢失，地图、棋子、HP、AC 和联机功能不受影响。

## M9：回合权限锁定与本回合移动路径

### M9.1 功能边界与权限语义

- 自由模式维持现状：观战台玩家可以移动自己名下的全部棋子，主控台可以移动任意棋子。
- 回合制下，观战台玩家只有同时满足“棋子属于自己”和“棋子是当前先攻角色”时才能拖动该棋子；玩家拥有多个角色时，未轮到的其他角色全部锁定。
- 主控台是 GM 权威端，始终可以拖动任意角色，不受当前先攻限制。
- 本阶段的回合锁只约束棋子的拖动和地图位置；HP、HP 上限与 AC 仍按现有所有者权限修改，避免角色在别人的回合受到伤害时无法记录数值变化。
- 当前先攻条目必须关联有效的 `tokenId`。手动创建但未关联棋子的条目处于行动中时，观战台暂时无人可以移动棋子，主控台仍可照常操作。
- 骑手和坐骑按同一个控制组判断：如果当前先攻是骑手，允许其玩家拖动承载该骑手的坐骑；如果先攻条目直接关联坐骑，也按坐骑所有者或其骑手所有者判断。
- 前端的禁用样式只负责提示，真正权限必须在 `主控台/联机服务器.py` 的 `/api/action` 上再次校验，伪造请求也不能绕过回合锁。
- 服务器按失败原因返回明确提示，例如“只能操作自己名下的棋子”“尚未轮到这个角色”或“回合已经变化，请重新操作”。
- 当前联机仍是朋友间使用的信任模型，玩家身份来自观战台填写的名字；M9 能阻止越回合移动，但不负责防止有人故意冒用另一个玩家名。若以后开放给陌生人，再增加由服务器签发的玩家口令或会话令牌。

### M9.2 回合版本与路径状态模型

在现有 `encounter` 上增加回合版本和当前路径：

```js
encounter: {
  // M8 现有字段
  turnSerial: 1,
  turnPath: {
    mapId: null,
    tokenId: null,
    points: []
  }
}
```

- `turnSerial` 是单调递增的回合版本号。点击“下一位”、手动指定当前角色、重置战斗、清空先攻、删除当前行动条目或切换为自由模式时都递增。
- 每个观战台移动请求必须携带拖动开始时读取到的 `turnSerial`；服务器只接受与当前权威值相同的请求。
- 该版本校验用于解决拖动竞态：玩家开始拖动后，如果主持人已经换人，即使玩家随后松开鼠标，旧回合请求也会被拒绝。
- `turnPath` 保存当前回合已经确认的路径，`tokenId` 是实际在地图上移动的棋子；骑乘状态下可以是承载当前骑手的坐骑。
- 每次进入新回合时把 `turnPath` 原子地重置为空，再广播新的 `turnSerial`，避免客户端短暂看到“新角色 + 上回合路径”的混合状态。
- 旧存档缺少这两个字段时自动补为 `turnSerial: 1` 和空路径；路径属于临时战斗显示数据，不需要永久写入战役存档，但必须存在于联机服务器当前状态中，以支持观战页刷新和断线重连。

### M9.3 服务器权威校验

将现有笼统的 `can_control()` 拆分为“所有者判断”“当前回合移动判断”和“允许编辑 HP/AC”三层，移动请求按以下顺序处理：

1. 检查玩家名、地图、棋子和请求结构是否有效。
2. 检查棋子所有者或骑手/坐骑控制组是否与玩家一致。
3. 自由模式直接沿用所有者移动权限；回合制继续检查当前先攻条目的 `tokenId` 是否与该控制组匹配。
4. 检查请求携带的 `turnSerial` 是否等于服务器当前值。
5. 检查终点和路径点均为有限数字、位于地图边界内，且路径所属地图和棋子与移动请求一致。
6. 校验通过后，在同一把服务器锁内更新棋子位置、追加路径并写入玩家动作序号，然后通过 SSE 广播一次确认动作。

为防止异常请求拖慢联机：

- 单次拖动路径最多接收 60 个点，整回合最多保留 200 个点。
- 超出单次上限时先在观战台简化；服务器仍执行硬上限和数据类型校验。
- 服务器保存的是校验、裁剪后的终点和路径，不直接信任浏览器提交的坐标。
- `moveToken` 动作历史必须同时保存 `turnSerial` 和路径片段，使主控台下一次上传快照时可以像现有玩家移动一样重放未合并动作，不能被旧快照覆盖。
- 主控台 SSE 客户端收到已确认的玩家移动后，要把路径片段同步并入本地 `encounter.turnPath`；主控台断线重连时也要从 `/api/state` 恢复当前路径，避免下一次主机推送把服务器路径清空。

### M9.4 观战台拖动与回滚

- 在观战台集中实现 `canPlayerMoveToken(token, encounter, playerName)`，渲染、鼠标指针、拖动开始和发送请求统一调用，避免界面显示可拖但实际请求必然失败。
- 当前可操作棋子增加暖金色行动光圈和轻微高亮；自己拥有但尚未轮到的棋子显示锁定状态，鼠标为不可拖动样式。
- 点击自己拥有但未轮到的棋子时显示“尚未轮到该角色”；其他玩家的棋子仍按现有只读方式处理。
- `pointerdown` 时记录棋子起始坐标、当前 `turnSerial` 和本次路径草稿；`pointermove` 只做本地乐观移动和路径绘制。
- 网格吸附开启时，仅在棋子进入新的格心或大型棋子的有效吸附点时添加路径点；自由移动时按最小地图距离采样，并在松开时做折线简化。
- `pointerup` 后一次性发送“终点 + `turnSerial` + 路径片段”，不按鼠标移动频率发请求。
- 如果服务器拒绝、请求失败、指针取消或请求等待期间收到更高的 `turnSerial`，棋子回到拖动开始位置，丢弃本地草稿并显示原因。
- 请求成功后，用服务器广播的裁剪后坐标和路径替换本地乐观结果，确保所有页面最终一致。

### M9.5 路径绘制与同步

- 在地图世界容器中增加独立的 `turn-path-canvas`，放在地图材质与棋子之间，并设置 `pointer-events: none`，不影响点击、拖动、缩放和绘制工具。
- 当前拖动玩家实时看到本地路径草稿；松开并通过服务器校验后，主控台和所有观战页收到同一个路径片段并追加到本回合路径。
- 多次拖动同一行动角色时，路径在当前回合内累计；首次确认移动记录起点，后续片段从上一终点连续追加，并去掉重复相邻点。
- 路径使用半透明暖金线、起点圆点和当前端点；当前控制玩家看到较亮样式，其他观战者与主控台看到较柔和样式。
- 路径坐标保存在地图世界坐标中，Canvas 跟随现有地图缩放和平移，不为视口变化重新生成联网数据。
- 主控台移动当前行动角色或其骑乘控制组时也记录路径；主控台移动非当前角色仍然允许，但不写入本回合路径。
- 主控台动作不经过玩家 `/api/action` 权限接口，因此需要在本地移动结束时更新路径、递增状态脏标记并随正常状态推送同步。

### M9.6 路径清除时机

以下操作必须通过同一个 `beginNewTurn()` 或 `clearTurnPath()` 入口处理，避免遗漏：

- 点击“下一位”并切换到下一个先攻条目。
- 主持人手动点击另一个条目指定当前角色。
- 重置轮次、重置战斗或清空先攻列表。
- 删除当前行动条目、删除当前行动棋子或把该棋子移出当前地图。
- 从回合制切换到自由模式。
- 载入另一个战役或替换当前联机状态。

只展开/折叠流程条、切换“先攻 / 时间”标签、调整世界时间或修改先攻值排序时不清除路径；如果排序后当前条目不变，仍视为同一回合。

### M9.7 实施顺序

1. 扩展 `defaultEncounterState()`、`normalizeEncounter()` 和 `publicEncounterState()`，加入 `turnSerial`、空路径与旧状态兼容。
2. 在 `主控台/app.js` 集中实现回合切换和路径清除入口，并让所有换人、删除、重置、模式切换路径统一经过该入口。
3. 重构 `主控台/联机服务器.py` 的权限与动作校验，先完成服务器端回合锁和过期请求拒绝。
4. 改造 `主控台/观战.html` 的可操作状态、拖动起点记录、失败回滚和 `turnSerial` 请求字段。
5. 增加本地路径采样、折线简化、独立 Canvas 和视觉样式，再把确认路径接入 SSE 动作广播。
6. 让主控台本地拖动当前角色也记录路径，并补齐主控台 SSE 合并、服务器动作重放、刷新与重连恢复。
7. 完成权限伪造、换回合竞态、骑乘、多观战页和性能回归，再决定是否需要可选的路径长度/距离统计。

### M9.8 验收标准

- 玩家拥有两个角色时，自由模式可移动两者；回合制只可移动当前先攻对应角色。
- 玩家伪造 `/api/action` 移动自己名下但未轮到的角色，服务器返回 403，棋子和路径均不改变。
- 主控台在任何模式下都可以移动任意角色；只有当前行动角色或其骑乘控制组的移动进入本回合路径。
- 玩家拖动途中主持人换回合，旧请求因 `turnSerial` 过期被拒绝，观战台棋子回滚到拖动起点。
- 同一角色在一个回合内多次移动，路径连续累计；网格与自由移动两种模式下都不会产生明显重复点或过量数据。
- 点击“下一位”、手动换人、删除当前角色或切换自由模式后，所有已连接页面在一次状态/动作推送内清空旧路径。
- 观战页刷新、SSE 断线重连和主控台短暂断线后，仍能恢复当前回合、移动后坐标与已确认路径。
- 骑手作为当前行动角色时，其玩家可以移动对应坐骑；不能借此移动不属于该控制组的其他大型棋子。
- 路径点被篡改为越界、`NaN`、无穷值或超大数组时，服务器安全拒绝或裁剪，不污染权威状态。
- 连续拖动 20 次只产生每次一次动作提交，不出现逐帧网络请求，也不会让地图拖动和缩放出现明显卡顿。

## 首个交付里程碑

第一轮实施范围控制为：

1. 重排主控台顶栏和左右侧栏。
2. 增加地图悬浮工具条。
3. 建立统一 UI 组件与状态样式。
4. 抽出地图工坊和主控台共用的 Canvas 材质渲染器。
5. 建立 AI 材质生成规范、素材目录、预加载和缓存机制。
6. 完成石地、木地、草地、泥土、水面和石墙六种 AI 材质及其变体。
7. 由 Canvas 完成六种材质的格子铺设、边缘连接和稳定变体选择。
8. 保持地图存档格式和当前联机协议不变。

完成首个里程碑后，再根据实际观感决定后续材质更偏向“写实旧地图”“精致游戏棋盘”或“手绘跑团地图”。
