# 🐉 龙与地下城 · 跑团台

一个跑在自己电脑上的 D&D 跑团辅助小工具，双击即可打开，不需要联网、不需要安装任何东西。适合你把屏幕共享给队友一起玩。

## 项目目录结构

```
桑哆尔的世界/
├── 启动桑哆尔.command / 启动桑哆尔.bat   一键启动服务器并打开主控台
├── three.min.js / 骰子动画.js            3D 骰子动画共享资源
├── 主控台/                 主控台 + 先攻计时程序与状态
│   ├── 主控台.html          跑团主程序（双击打开）
│   ├── 观战.html            玩家模式页面（玩家通过服务器地址打开）
│   ├── 联机服务器.py        联机服务器（启动器/命令行调用）
│   ├── 先攻计时.html        先攻条 + 时间条（与当前战役自动绑定）
│   ├── app.js / style.css
│   └── 状态/
│       ├── 当前状态.json     绑定项目文件夹后自动写入（临时进度）
│       └── 战役/<战役名>/存档.json  每个战役一份，自动写盘
├── 棋子库/                 棋子库程序 + 数据 + 立绘
│   ├── 棋子库.html          棋子管理程序（双击打开）
│   ├── 棋子库数据.js         棋子永久存档（📌 绑定后自动写入）
│   └── 立绘/                高清立绘原图，按棋子分类细分文件夹
│       ├── 妖精/地精/… 龙类/红龙/… 元素/… 巨人/… NPC/… 角色/…
├── 地图/
│   ├── 地图工坊.html        地图制作程序（双击打开）
│   ├── 使用说明.txt
│   └── 战役地图/            所有 .地图.json 数据，按战役分类
│       └── 短团·烬鳞讨伐/…
├── 备份/                   历史存档备份（程序或人工整理时放入）
├── 音乐/                   BGM 音乐文件（主控台左侧绑定后自动列出）
├── 备团/
│   ├── 备团流程.md
│   ├── 模板/                剧情模板 / 速查卡模板
│   └── <团名>/              每个团的备团剧情与速查卡
└── 使用说明.md
```

## 怎么打开

双击 `主控台/主控台.html`，用浏览器（推荐 Chrome / Edge / Safari）打开即可。
封面使用 `主控台/封面背景.png`（或 `封面背景.jpg`）作为背景图，程序自动加暗色遮罩保证文字清晰；不放图时使用默认暗色渐变。

打开后会先出现**封面页**：⏯ 继续战役（直接回到上次关闭时的进度，含当前战役）/ 🆕 新建战役（空白开始、保存为独立战役）/ 📂 载入战役（打开已有战役）/ ⚡ 临时战役（直接开玩、不写入战役存档）。

## 主要功能

| 功能 | 说明 |
| --- | --- |
| 🗺️ 地图 | 点击「上传地图」选择图片，或直接把图片拖进窗口；也可以按 Cmd/Ctrl + V 粘贴截图。`地图/战役地图/` 里的战役地图可用「从地图文件夹加载」批量加入 |
| 🧩 拼接地图 | 点「拼接地图」可以把多张地图拼成一张大地图（左右并排 / 上下叠放 / 2×2），支持图片或地图工坊文件 |
| 🏢 多地图（楼层） | 顶栏可以新建、切换、重命名、删除多张地图，每张地图有独立的图片、网格和棋子；点 ‹ › 或按 `[` / `]`（PageUp/PageDown）快速切换 |
| 📏 网格与吸附 | 网格固定为每格 50 像素（= D&D 5 尺），可开关；打开「吸附」后 1×1 棋子自动对齐到格心，2×2 大棋子对齐到网格线交点（正好占 4 格） |
| 🏷️ 名字开关 | 顶栏「名字」开关可以随时显示/隐藏棋子名字 |
| ✏️ 地图编辑 | 左侧面板轻便地图编辑器：**39 种地块全部带名字**，分门别类展示（🟫 地面 / 🧱 墙与障碍 / 🚪 门与通道 / 🪑 家具物品 / ⚠️ 机关与自然），选地块后点击或拖动直接涂抹，可撤销；「清除」只擦掉编辑器涂的临时格子、恢复原图，不会破坏地图 |
| 🎖️ 棋子库 | 左侧面板直接用共享棋子库预设（一键「放到地图」），支持按**两级分类**（妖精/地精、龙类/红龙…）和类型筛选；每个棋子带 HP、**AC** 和**职业**；点「⟳ 同步」读取最新数据 |
| 🕹️ 格子交互 | 门格子点击循环「关→开→锁」，宝箱「关→开」，陷阱「未触发→已触发」，火把「灭→燃」；普通格子点击加/去黄色标记框；右键反向切换 |
| 🧬 选中单位 | 右侧「选中单位」卡片：名称 + **类型（玩家/敌人/中立NPC/友好NPC 可随时下拉更改，颜色圆环同步：中立 NPC 黄、友好 NPC 绿）** + 生命值 / **AC·先攻·等级**（自动计算；顶部大号等级徽章，人物卡里可改 1–20 级）→ **生命骰（按职业自动 d6–d12，数量＝等级；可点格子记已用、短休掷 1 颗恢复生命、长休恢复全部）** → **等级系统（熟练加值、职业资源上限、术法点、咒术师契约环位全部随等级自动绑定）→ 豁免（六项，按 2024 规则职业熟练两项自动计算）** → **职业系统**（13 个职业 + 各职业子职业可选；**法术环位类型由职业/子职业自动决定**——战士·奥法骑士、游荡者·诡术师拥有正常环位，战斗大师拥有战技骰；诗人/牧师/德鲁伊/游侠/圣武士/法师为正常环位，术士为术法点，咒术师为契约环位；专属资源：狂暴次数、气点、激励骰、引导神力、圣疗点数、奥术恢复、战技骰；**浅蓝＝剩余，灰＝已用，从右往左扣减，数字显示剩余/最大**；「无职业」保留手动选择）→ 六属性 → **18 项技能熟练（点击循环：未熟练 → 熟练 → 精通）** → 熟练/速度/先攻调整 → **状态栏**（21 种状态分 5 类）→ 装备 → 攻击；底部有**骑乘**、图标、存入棋子库、删除 |
| 🖍️ 战术涂鸦 | 画笔/直线/箭头/圆圈，可换颜色和粗细；「选择」工具可拖动已画形状，Delete 删除；每张地图独立保存 |
| 🌫️ 迷雾 | 顶栏打开「迷雾」后未探索格子变黑；「揭开/遮回」笔刷（1/3/5 格）逐步探索，或「全部隐藏/全部显示」 |
| 🗂️ 战役管理 | 存档菜单「战役管理」：不同团整份状态（地图/棋子/迷雾/涂鸦/先攻）分别存成战役；**绑定项目文件夹后每次改动自动写盘**，不用再手动点保存 |
| 🐎 骑乘系统 | 1×1 棋子可骑乘 2×2 大型棋子（座狼、人马…），骑手显示为坐骑上的小圆并跟随移动，坐骑名字在下方、骑手名字在上方 |
| ✋ 拖动棋子 | 直接拖动棋子移动位置；右键棋子可快速标记/取消死亡 |
| 🩸 血量与状态 | 右侧面板扣血/回血/回满，或勾选状态；状态以徽章显示在棋子上，同名棋子自动编号 |
| ⚔️ 先攻条 | 屏幕底部加高横条：添加参与者按数值排序，「下一回合」自动轮转并计回合数 |
| 🎲 骰子 | 常用骰子一键掷出，支持 `2d6+3` 表达式；d20 有优势/劣势按钮。**GM 掷骰仅自己可见，不会广播**；玩家在玩家模式页的掷骰会实时广播给所有人 |
| 📖 常用网站 | 左侧最下方「📚 常用网站」折叠模块，默认 5E 不全书，可自己增删常用网站（主控台与先攻计时共用） |
| 🎵 BGM | 左侧「棋子库」与「常用网站」之间的音乐模块：绑定 `音乐/` 文件夹后自动列出所有音乐，支持播放/暂停/上一首/下一首/音量记忆/单曲循环；开启玩家模式后，**播放的音乐会自动上传到服务器并广播，玩家端同步听到同一首歌**（首次播放被浏览器拦截时，玩家点一下页面即可开始跟随） |
| 💾 保存 | 自动保存到本机浏览器；**绑定项目文件夹后自动写盘**；「完整备份」把全部数据一套带走 |

## 存档、备份与战役（重要）

数据默认存在浏览器里；为了让项目文件夹成为真正的“资产仓库”，主控台提供了两种方式：

**📌 绑定项目文件夹（推荐）**
1. 主控台「💾 存档」→「📌 绑定项目文件夹」，选择整个 `桑哆尔的世界` 文件夹（只需授权一次）；
2. 之后每次改动自动写盘：
   - `主控台/状态/当前状态.json`（临时进度）
   - `主控台/状态/战役/<战役名>/存档.json`（每个战役一份，随开随存）
3. 换电脑时把整个项目文件夹拷走，浏览器数据清空也不怕。

> 绑定后，打开「战役管理」会自动与文件夹**双向同步**：双击打开主控台和通过 `http://localhost:8090` 打开主控台，只要各自绑定同一个文件夹，看到的战役就是同一批；战役「导出」的文件也会同时存入 `主控台/状态/导出/`。

**📦 完整备份 / 恢复**
- 「完整备份」导出 `桑哆尔完整备份.json`，包含：主控台状态、**全部战役**、棋子库、先攻计时（每个战役一份）、常用网站；
- 「恢复完整备份」一键还原以上全部数据。这是搬家的最省心方式。

**🗂️ 战役管理**
- 不同团的整份状态（地图/棋子/迷雾/涂鸦/先攻）分开保存；
- 绑定项目文件夹后，每个战役自动写入 `主控台/状态/战役/<战役名>/存档.json`，打开战役管理时双向同步；未绑定时仍存在浏览器（建议手动点「保存」）；
- 先攻计时程序与当前战役自动绑定：主控台切到哪个战役，先攻计时就加载哪个战役的数据。

**🖼️ 高清立绘**
- 立绘原图统一放在 `棋子库/立绘/`（按分类细分）；浏览器里的高清图只是缓存；
- 上传图片最高保留 2048px；顶栏「名字」开关右侧有「高清」开关：**打开＝优先直接读取 `棋子库/立绘/` 原图，找不到原图时使用随棋子数据保存的 512px 高清展示版；关闭＝切换为 128px 轻量版**。因此直接双击和 `localhost` 打开不再依赖同一份浏览器高清缓存。
- 棋子库顶部「🎨 立绘工作台」提供完整流程：选择棋子 → 补充外观 → 复制标准绘图提示词给 Codex/图像模型 → 绑定一次 `棋子库` 文件夹 → 导入成图。导入后自动把原图归档到 `立绘/<分类>/`，同时生成 2048/512/128 三档缓存并绑定棋子；未绑定文件夹时会以内嵌 512px 高清版作为后备。

## 💻 跨平台（Windows / 换电脑）

整个文件夹是纯网页应用，**不需要安装任何东西**。打包带到 Windows 上，双击 `主控台/主控台.html` 即可使用（推荐 Chrome / Edge）。

- **能用什么**：主控台、地图工坊、棋子库、先攻计时全部功能都正常；地图文件、导入导出、战役导出/导入都是标准 JSON，跨系统通用。
- **注意浏览器**：程序共用数据依赖浏览器本地存储。Chrome / Edge（Windows 自带）完全支持；如果默认浏览器是 Firefox，棋子库程序与跑团台的「自动互通」可能失效，请用 Chrome / Edge 打开。
- **换电脑三步**：
  1. 主控台「💾 存档」→「📦 完整备份」导出（或已绑定文件夹则直接拷贝项目文件夹）；
  2. 把整个项目文件夹（含 `棋子库/`、`地图/`、`主控台/`）拷到新电脑；
  3. 新电脑打开主控台 →「📥 恢复完整备份」；浏览器图片缓存丢失时，可在棋子库运行一次「🖼️ 重建高清缓存」。
- 文件夹名、文件名包含中文没关系，Windows 正常支持；用系统自带的压缩/解压工具即可（避免乱码选带 UTF-8 的解压工具）。

## 🗺️ 地图与地图工坊

**地图数据**放在 `地图/战役地图/`，按战役分文件夹（当前有 `短团·烬鳞讨伐/`）：

- 在地图工坊画好地图导出后，把 `.地图.json` 放进对应战役的文件夹；
- 打开跑团台，点「🗺️ 地图」→「📁 从地图文件夹加载（多选）」，一次选中多张即可批量加入；**同名地图会自动替换**（更新底图和物件、保留地图上的棋子和迷雾），不会产生重复地图；重新导出一版地图后，再走一遍文件夹加载即可刷新；
- 当前四张战役地图：林地·鸦羽谷道 / 营地·碎颅丘 / 酒馆·桑哆尔之歌 / 龙巢·烬鳞矿窟；
- 这些文件是纯格子数据，体积很小（不带图片），加载后由跑团台直接渲染。

**地图工坊**：双击 `地图/地图工坊.html`。D&D 规则中 **1 格 = 5 尺**，工坊里画的每一格，导入跑团台后就是 5 尺一格。

### 怎么用

1. 双击打开 `地图/地图工坊.html`；
2. 用画笔、直线、矩形、填充、橡皮、拾取工具画地图；右侧状态栏显示地图总尺寸（如 24×18 格 = 120×90 尺）；
3. 想快速出图，可以用「随机生成」：地牢（随机房间+走廊+门，可调参数/种子）、迷宫、模板（地牢大厅/野外营地/洞穴/酒馆）；
4. 点「💾 保存」得到 `.地图.json` 文件；以后点「📂 打开」继续编辑；
5. 点「📤 导出到跑团台」得到同格式文件；
6. 打开跑团台，点顶栏「📤 导入地图」选择该文件——新增一张地图并自动设好网格（每格 = 5 尺）。

### 小贴士

- 也可以「导出 PNG」；跑团台网格线是它自己画的，导出的 PNG 不带网格。
- Ctrl/Cmd + Z 撤销，Ctrl/Cmd + Shift + Z 或 Ctrl + Y 重做；编辑草稿自动存浏览器。
- 快捷键：Ctrl/Cmd + S 保存，Ctrl/Cmd + O 打开。
- 迷宫和地牢支持「种子」复现；墙壁智能衔接；画布 − / ＋ / ⛶ 或 `+` / `-` / `0` 缩放；「🖐 平移」或中键拖动。
- 框选工具可复制/剪切/粘贴/旋转（Ctrl/Cmd + C / X / V）；「🧩 大物件」提供 2×2 / 3×2 整体物件，随地图导出。
- 「➕ 存为模板」可把整张地图存成模板复用。
- 想拼更大场景时，生成几张地图后在跑团台「🧩 拼接地图」合并。

## ⏱️ 先攻计时（与战役绑定）

双击 `主控台/先攻计时.html`。它是独立的先攻条 + 时间条程序，跑团时开在另一个窗口或投影给玩家看：

- **与战役绑定**：程序会自动读取主控台当前战役——主控台切到哪个战役，这里就显示哪个战役的数据（顶栏有「当前战役：××」标签）；不同战役的先攻/时间/限时/天气互不干扰。
- **先攻列表**：手动添加参与者（名称/先攻/HP/AC/类型颜色），自动排序；「下一名 / 上一名」切换回合；勾选「跳过」可略过昏迷/死亡单位；方向键 ← → 切换。
- **从棋子库导入**：「📚 从棋子库」搜索共享棋子库，点击即加入。
- **时间条**：游戏内日期（第 X 年/周/天）+ 时刻，支持 +6 秒/±1/10 分钟/±1 小时、饭点跳转（已过自动跳次日）、短休（3 秒进度动画 +1 小时）、长休（8 秒 +8 小时）；自定义可加任意分钟/小时/回合。
- **天气系统**：9 种天气按天保存，🎲 随机。
- **限时条**：显示在主时间条下方的独立轨道上，时间走完自动消失并提示，点 × 直接删除。
- 数据按战役自动分存；顶部「导出/导入」可单独备份当前战役的先攻计时数据。

## 🎖️ 棋子库（程序 + 数据 + 立绘一体）

双击 `棋子库/棋子库.html`。整个棋子库体系都在 `棋子库/` 文件夹里：程序、永久数据、立绘原图三位一体。

- **两级分类**：如「妖精/地精」「龙类/红龙」「巨人/巨魔」，左侧树形归组；
- **永久保存**：第一次点「📌 永久保存」选择 `棋子库/棋子库数据.js`，之后每次增删改自动写入；换电脑拷贝整个项目文件夹即可；
- **立绘**：原图统一放在 `棋子库/立绘/`，按分类细分文件夹（如 `立绘/妖精/地精/地精喽啰.png`）；路径统一登记在 `棋子库/立绘清单.js`；
- **绘制与入库**：点「🎨 立绘工作台」，选择要绘制的棋子并填写外观要求，复制生成好的提示词；成图后回到同一窗口点「导入成图」，立绘会立即绑定并写入永久棋子数据；
- **重建高清缓存**：选择 `棋子库` 文件夹，按立绘清单批量匹配原图，在当前浏览器重建 2048/512/128 三档缓存；它不是 AI 超分辨率；
- **主控台高清显示**：新版首次启动默认开启，直接读取项目立绘原图；关闭后才使用 128px 缩略图；
- 顶部「导出数据/导入数据」只迁移棋子数据。完整项目备份还要保留 `棋子库/立绘/` 和 `棋子库/立绘清单.js`。

### 与跑团台怎么互通

两个程序共用同一份本机数据（浏览器）。用法：

1. 打开 `棋子库/棋子库.html`，分门别类整理好你的棋子；
2. 打开跑团台 `主控台/主控台.html`，左侧「棋子库」自动载入预设；
3. 改了内容后回跑团台点「⟳ 同步」（或切回窗口）即可更新；
4. 点「放到地图」把预设放到当前地图，1×1 落格心、2×2 落交点。

> 注意：两个程序要在**同一个浏览器**里打开才能互通。换浏览器或换电脑时，用「完整备份」或「棋子库导出」。

## 📡 简易联机（你的电脑当服务器）

把主控台画面实时推给同一 WiFi 下的玩家（**玩家模式**），不需要公网：

1. **一键启动**：双击项目根目录的「启动桑哆尔」（macOS 是 `.command`，Windows 是 `.bat`），会自动启动服务器并打开主控台。也可以手动运行 `python3 主控台/联机服务器.py`（默认端口 8090，可加参数改端口）。看到「玩家请打开: http://192.168.x.x:8090/主控台/观战.html」即成功。
2. **主机推送**：打开 `主控台/主控台.html`，点顶栏「📡 联机」——可以先点「🔍 检测服务器」确认在线，或「📋 复制启动命令」；再点「📡 开启玩家模式（主机推送）」开启。之后地图/棋子/迷雾/先攻/法术环位的改动**最快约 0.3 秒自动推送**给所有玩家（没有改动时每 2.5 秒保活一次）；需要立刻同步时点「🔄 更新数据（立即推送）」。开启后可用「🔗 复制玩家观看地址」把链接直接发给玩家。
3. **玩家观看**：同一 WiFi 下用浏览器（Chrome/Edge）打开 `http://<主机IP>:8090/主控台/观战.html`，实时看到地图、棋子（含骑手）、迷雾和先攻条。
4. **关闭**：再点一次「📡 联机 → ⛔ 关闭玩家模式」即停止推送；服务器窗口按 Ctrl+C 退出。
5. **自动恢复**：开启状态下关闭主控台再打开，会自动恢复联机推送；如果先开主控台、后启动服务器，也会自动连上，无需再点按钮。

说明：
- 只适合同一局域网；公网/异地需另行方案。
- 玩家模式页默认只读；按下面「玩家操作自己的棋子」分配所属玩家后，玩家可以操作自己名下的棋子。
- 迷雾跟随主机显示：主机揭开多少，玩家看到多少。
- 关闭推送时玩家页面会显示「连接中断，重连中…」，重新开启后自动恢复；服务器短暂断线时主机约 1.5 秒自动重试。
- 服务器零依赖，只要电脑上有 Python 3（macOS 自带）。

### 🎮 玩家操作自己的棋子

想让自己操作棋子，按下面两步：

1. **主机分配**：在主控台点选棋子，在人物卡「所属玩家」一栏填玩家名字（留空 = GM 控制）。
2. **玩家认领**：玩家打开玩家模式页后，在顶部「你的名字」填**同样的名字**。

之后：
- 自己名下的棋子会显示**绿色高亮**，可以直接拖动（自动吸附格子），点击棋子会弹出状态菜单（死亡、中毒、倒地、昏迷等）；
- 骑乘状态下的骑手也可以拖动整匹坐骑；
- 地图可以**自由缩放平移**：滚轮或右上角 ＋/－ 缩放，按住空白处拖动平移，「⛶」一键回到整图；
- 点击自己的棋子可以**直接编辑**：**血量、生命骰（掷骰恢复、记录已用、长休恢复全部）、六属性、熟练/速度/先攻调整、法术环位（浅蓝＝剩余 / 灰＝已用，从右往左扣减，数字显示剩余/最大）、职业资源、装备（增删/改数值）**，并可直接切换状态（**21 种状态分 5 类，与主控台一致；力竭等可叠加状态左键升级、右键降级**）；职业和子职业由 DM 指定，玩家不可改；
- 玩家的每次修改会**自动实时同步到主控台**（GM 可以看到最新数值并保存），主控台也可以在人物卡里继续修改；
- 可以看到**等级、豁免、18 项技能熟练**（只读，由 DM 在主控台设置）以及 **AC、攻击命中/伤害**等自动计算结果；
- 左侧最下方新增**常用网站**模块（与主控台同步，默认含 5E 不全书，主机可增删）；
- 主机播放 BGM 时玩家端会自动跟随播放；如果被浏览器静默拦截，点一下页面任意处即可开始。
- 玩家页布局与主控台类似：**左侧**是骰子和战术涂鸦（涂鸦实时同步给所有人），**底部**是先攻条，**右侧**点自己的棋子展开属性；
- 点右上角「🎲 骰子」可以掷骰（常用骰、优势/劣势、自定义如 `2d6+3`），结果实时广播给主控台和所有玩家，**所有人屏幕上都会出现对应面数的 3D 骰子动画**（d20 二十面体、d12 十二面体、d6 立方体等）；
- GM 在主控台掷骰是**保密骰**，只显示在自己屏幕，玩家看不到；
- 主机和所有玩家实时同步，不需要任何额外操作；
- 没有填所属玩家的棋子只有 GM 能操作；
- 玩家操作只在主机开启「玩家模式」时可用。

## 备团

`备团/` 是完整的备团内容仓库：

- `备团/备团流程.md`：备团流程说明；
- `备团/模板/`：剧情模板、速查卡模板、地图生成脚本模板；
- `备团/<团名>/`：每个团自己的备团剧情与速查卡（当前有 `短团·烬鳞讨伐/`）。

## 其他小贴士

- **🎵 BGM 用法**：左侧展开 BGM 卡片 →「📂 绑定音乐文件夹」选择项目里的 `音乐/` 文件夹（授权一次），之后把音乐文件放进该文件夹、点卡片即可自动列出；也可以「🎵 选择音乐文件」临时播放（仅本次会话）。音量会自动记住。
- **拖动地图性能**：拖动/平移地图时程序会自动临时隐藏棋子名字和光效阴影、松手立即恢复（不影响任何数据），保证拖动流畅；高清开关默认关闭也是同理。
- 选中棋子后按 `Delete` 键可直接删除，按 `Esc` 取消选中。
- 地图图片如果超过约 5MB，自动保存可能失败（浏览器限制），建议先压缩。
- 「上传地图」会替换当前地图的图片；「导入地图」（地图工坊文件）会**新增**一张地图并切换到它。
- 力竭状态左键升级、右键降级；右键棋子可快速标记/取消死亡。
