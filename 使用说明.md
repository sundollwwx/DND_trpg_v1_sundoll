# 桑多尔之歌

这是一个给熟人小团使用的本机 Web 跑团桌面：主机在自己的电脑上开服务，朋友用浏览器进入玩家页，无需安装软件或注册账号。

棋子是纯地图单位，只保留当前 HP、最大 HP 和 AC；不包含职业、法术、属性、技能、装备等自动规则计算。状态和先攻仅用于记录与展示。

## 主要功能

- 地图工坊文件与普通图片载入、多地图预览、排序、删除、独立战术格距与吸附
- 棋子放置、移动、骑乘、所属玩家和棋子库
- 棋子 HP / 最大 HP / AC
- 主控台与玩家共享战术涂鸦、普通/优势/劣势骰子、GM 私密/公开骰子、BGM、常用链接
- GM 共享公告/手册、玩家在线状态、准备、测距和个人笔记
- 战役保存、读取列表与“存档”文件夹自动写盘
- 主机推送的玩家联机

## 一键上传 GitHub

先在主控台保存战役，再在项目根目录双击 `一键上传GitHub.command`（Mac）或 `一键上传GitHub.bat`（Windows）。需要已安装 Git、Python 3，并有该私人仓库的 GitHub 登录权限；首次连接可能需要完成 Git 的登录提示。

程序固定更新 `sundollwwx/DND_trpg_v1_sundoll` 的 `main` 分支，以当前电脑项目内容为准，包括代码、素材、正式存档、自动备份和本地删除的文件。缓存、日志等遵循 `.gitignore`；浏览器中尚未写盘的内容不在上传范围。成功后窗口显示远端核验过的版本号，按回车关闭即可。

如果另一台电脑已上传新版本，本程序会保留两边的提交历史，生成以当前电脑文件为内容的新版本；远端独有的文件也会从最新版移除。旧版本仍可通过 Git 历史找回。上传期间不要同时编辑或在其他电脑上传；如远端恰好发生新提交，普通推送会拒绝覆盖，重新运行即可。程序不会下载远端文件来替换本地，也不使用强制推送。

网络或认证失败时，错误会留在窗口中，本地文件和已创建的提交会保留；解决后重新双击即可。单个普通文件超过 100 MiB 时，会列出文件并停止，需要缩小该文件或配置 Git LFS。请勿同时运行多个上传窗口。

## 快速开团

1. 在项目根目录双击「启动桑多尔之歌」（macOS 为 `.command`，Windows 为 `.bat`），然后选择：

   - `1`：本地 / 同一 Wi-Fi / Radmin（推荐）
   - `2`：Cloudflare 公网 Tunnel

   也可以绕过交互菜单，直接运行服务器：

   ```bash
   python3 主控台/联机服务器.py
   ```

   默认端口是 `8090`；若端口被其他程序占用，双击启动器会自动寻找后续可用端口并打开正确页面。需要手动指定端口时可运行 `python3 launch_sundoll.py local --port 8092`。不要直接双击 HTML 文件启动联机。

2. 启动器会自动打开正确端口的主控台（默认是 `http://localhost:8090/主控台/主控台.html`）；随后在「📡 联机」中点击「开启联机」。

3. 把「复制玩家链接」得到的地址发给朋友，例如：

   ```text
   http://192.168.x.x:8090/主控台/玩家.html
   ```

4. 玩家第一次打开链接时输入自己的名字和房间码。之后会记住临时会话，刷新网页也不会丢失身份。

同一 Wi-Fi 下直接使用本机局域网 IP 即可。若通过 Radmin VPN 联机，朋友使用 Radmin 分配的虚拟网卡 IP；双方都需要允许该端口通过系统防火墙。

## 地图载入与管理

- 在左侧「地图」中打开地图列表，可以载入、预览、切换、重命名、排序和删除地图。
- 「载入地图」可同时选择 PNG、JPG、WebP、GIF 或地图工坊导出的 JSON。普通图片会进入确认页，每张图片独立成为一张地图，一次最多准备 30 张；地图工坊文件会保留地块类型、样式和状态数据。
- 普通图片在确认前可设置名称和“战术格距”。地图会按该格距始终显示网格，同时用于吸附、测距、施法范围、回合移动与每格 5 尺换算；界面不再提供隐藏网格的开关。
- 已载入的普通图片可在地图卡片上修改战术格距；地图工坊的格距由文件决定。
- 每张卡片都带删除、重命名、左右排序按钮和 `⠿ 拖动排序` 把手。顺序及每张地图的战术格距会随战役存档保存。

普通图片按原文件保存，不会为了缩略图而压缩。地图浏览中的小图只是运行时预览缓存，不写入存档，也不会改变玩家端收到的地图画质。批量导入大图会明显增加存档体积，建议先确认主页显示“存档已默认连接”再操作。

## 为什么这版联机更顺

- 只同步当前正在使用的一张地图，不再把全部楼层和棋子库一起发送。
- 地图和头像首次传到服务器后会变成可缓存的资源地址，不再在每条 SSE 消息中携带 Base64 大图片。
- 只有实际改动时才推送完整状态；玩家移动、HP、AC 和共享标注使用小型增量动作，不会再紧接着回传一份大快照。
- 服务器为每个状态和动作分配版本号；浏览器断线后会先补发遗漏动作，必要时再获取完整快照。
- 动作带有唯一编号，网络重试不会让同一个移动或回合结束执行两次。
- 地图和头像的联机副本会缓存到 `.sundoll-cache/联机资源`，服务器重启后仍可复用；它不是正式存档，删除后可以重新生成。

因此 Radmin 主要传的是地图操作和小型状态变化，通常比旧版稳定得多。第一次进入地图仍要下载一次地图图片；这属于正常加载，之后浏览器会复用缓存。

## 音乐与标签页声音

主控台会自动扫描项目中的 `asset/音乐`，不再要求每次绑定文件夹。`通用/` 里的音乐在所有战役出现；`战役/<战役ID>-<战役名>/` 只在对应战役出现，并可继续用 `环境/`、`战斗/`、`休息/`、`Boss/` 分类。切换战役会自动刷新，也可在“资源 → 音乐”手动点击“刷新”。项目音乐使用支持拖动进度的流式传输，不会先把整首复制到运行缓存。

玩家端“资源 → 房间音乐”会显示当前曲目、播放状态、音量和静音。若浏览器阻止自动播放，玩家点击一次“启用声音”即可；重新打开浏览器后可能需要再点一次，这是浏览器自身的声音权限规则。

没有本地音乐文件时，主控台可点击“广播标签页”，在 Chrome 或 Edge 的分享窗口中选择正在播放的 B 站标签页并勾选“共享标签页音频”。程序只通过 WebRTC 实时转发声音，不下载视频，也不传画面。局域网、Radmin 和多数普通家庭网络可以直接连接；Cloudflare Tunnel 只负责页面与信令，遇到严格 NAT 的跨公网玩家时，标签页直播仍可能需要后续配置 TURN 服务。请只播放自己有权使用的内容。

## 玩家权限

主机在右侧选中棋子后，进入「管理」，在「所属玩家」中选择在线玩家；选择「GM 控制」则取消玩家归属。玩家加入房间后，输入的名字会绑定到本次临时会话。主机仍然是最终控制者；玩家只能操作自己名下的棋子：

- 拖动移动
- 修改当前 HP、最大 HP、AC
- 使用普通、优势和劣势骰子
- 使用共享画笔、直线、箭头、圆圈、橡皮擦、撤销和一键清空；颜色及粗细与主控台一致

玩家页还可以使用在线玩家列表、准备/暂离、测距、个人笔记和结束回合。共享标注会同步到主控台及所有玩家；测距仍只在自己页面显示。回合制下，只有当前先攻角色的玩家可以移动该角色或其骑乘控制组、修改 HP/最大 HP/AC 并结束回合；非当前角色仍可查看，但操作控件会锁定。骰子、测距、笔记和共享标注不属于角色操控，可在回合外继续使用。主控台始终可以操作全部棋子。当前角色的本回合移动路径会同步显示给主控台和所有玩家，换回合、重置或退出回合制后自动清空。玩家角色与友好 NPC 的名字下会显示细血条；点击自己的棋子会打开右侧编辑栏，点击别人的友方棋子只会短暂显示公开信息卡。敌人和中立 NPC 不公开 HP/AC，也不显示血条。

其他棋子只有查看权限。

## 世界时间与气候

每天 08:00 自动推演一次天气。气候带与季节决定基础天气范围，前一天的天气和风力会通过马尔可夫链影响下一天，因此连续雨天、风暴转雨、降雪延续等过程会比完全随机更自然；温度也会保留前一天的惯性，并继续随每个小时的昼夜曲线变化。一次把时间向前调整多天时，系统会按天依次推演，而不是只抽取最后一天。

## 保存与旧数据迁移

正式存档始终是项目根目录中可直接查看和复制的 `存档` 文件夹，不会藏在服务器数据库里。通过 `启动桑多尔之歌.command`、`启动桑多尔之歌.bat` 或 `launch_sundoll.py` 打开主控台时，本机启动器会自动连接这个固定目录，无需每次在浏览器中重新选择。

如果绕过启动器直接打开 HTML，程序会退回浏览器文件夹模式：第一次点击「💾 存档」或「📂 读取存档」时选择项目中的 `存档` 文件夹。该退路主要供 Chrome / Edge 使用。

```text
存档/
├── 存档索引.json
├── 棋子库/
│   └── 棋子库.json
├── 战役/
│   └── <战役ID>-<战役名>/
│       ├── 当前存档.json
│       └── 自动备份/
```

「存档」保存当前战役；「读取存档」会列出该文件夹中的全部战役供选择。平时改动会自动更新 `当前存档.json`，手动存档和读档前会留下恢复版本，最多保留最近 10 份。棋子库是全局数据，只保存在 `存档/棋子库/棋子库.json`，不再复制进每个战役；重新连接已有 `存档` 时，以其中的正式棋子库为准，浏览器缓存只会在该文件尚不存在时用于首次迁移。项目内的 `asset/棋子库/默认预设.js` 仅用于第一次打开时提供默认模板，不是第二份存档。

主页的每张战役卡片都可点击「上传封面」或「更换封面」。PNG、JPG、WebP 原图会自动居中裁成 1600×900（严格 16:9），并压缩后独立保存在该战役目录中；封面不会塞进 `当前存档.json` 或自动备份，因此不会拖慢日常存档。点击「移除」只删除封面，不影响战役内容。以后用 AI 制作封面时请直接使用 `asset/地图/战役封面制作Prompt.md`，其中已经固定 16:9 画幅，避免上传时裁掉主体。

主控台开启玩家模式后，当前战役封面会自动转换成联机资源并显示在玩家端主页。玩家端封面卡片保持与封面文件一致的 16:9 比例，不再把画面裁成超宽条幅。玩家只能访问经过内容哈希处理的封面地址，不能借此浏览主机的 `存档` 文件夹。更新程序后请重新运行启动程序；新版启动器不会继续复用协议过旧的服务器进程。

主控台和玩家端左侧工具栏的最右侧都是「房间」。玩家端可在这里查看成员并设置自己的准备状态；主控台可查看房间码、所有成员的在线/离线与准备状态，并可直接开启或关闭联机、复制邀请链接。

浏览器存储只作为意外关闭时的应急恢复点；本机存档服务不可用、文件夹未授权或写入失败时，顶栏会明确显示降级状态。通过启动器打开时主机可使用常见现代浏览器；直接打开 HTML 的备用文件夹模式建议使用 Chrome 或 Edge。旧版战役文件仍可读取，读取后再次保存会自动升级为当前格式；旧目录不会自动删除。

## 静态资源位置

地图工坊、棋子立绘、地图材质和示例音乐统一放在 `asset/`：

```text
asset/
├── 地图/
│   ├── 地图工坊.html
│   ├── 材质/
│   └── 战役地图/
├── 棋子库/
│   ├── 默认预设.js
│   └── 立绘/
├── 音乐/
│   ├── 通用/
│   └── 战役/<战役ID>-<战役名>/
└── 界面/
    └── 状态图标-v1.png
```

「存档」仍单独放在根目录，不属于资源文件；主控台和玩家页会自动从新的 `asset/` 路径读取立绘与地图材质。

## Windows / macOS 联机

- 主机可以使用 Windows 或 macOS；朋友只需浏览器，不需要安装 Radmin。
- 同一 Wi-Fi 下，使用启动页显示的局域网地址。
- Radmin 仍然只是 Windows 玩家之间的可选网络方式，Mac 不应依赖它。
- 如果朋友不在同一网络，双击同一个「启动桑多尔之歌」入口并选择 `2`。启动器会使用 Cloudflare Quick Tunnel，把本机服务临时转成 HTTPS 地址；`cloudflared` 只需要安装在主机电脑，朋友仍然只用浏览器。
- 启动器识别出 `https://...trycloudflare.com` 后，会直接打印包含玩家页和房间码的“玩家完整地址”，无需再手动拼接路径。
- 不建议直接把 8090 端口映射到公网；Tunnel 关闭后外网地址也会失效。需要固定地址时，再单独配置 Cloudflare Named Tunnel。
- 两个平台共用 `launch_sundoll.py` 的启动逻辑；它会确认 `/api/info` 返回的是本项目服务后再打开主控台，端口冲突和启动失败不会再藏在最小化窗口里。
- 正常开团不需要填写服务器地址或手动推送；启动器和当前页面地址会自动决定联机服务器。旧版保存过的服务器地址及 `?server=...` 参数仍保留兼容读取。

### 联机权限和骰子

- 玩家必须先输入名字和房间码加入临时会话；没有有效会话不能移动、改 HP/AC、使用共享标注、掷骰或结束回合。
- 主控台始终可以操作所有棋子。选中棋子后，在「所属玩家」中选择在线玩家即可分配；留空表示 GM 控制。
- 回合制的移动、HP/AC 修改和结束回合都会由服务器核对当前先攻及 `turnSerial`；修改请求不能靠篡改网页或延迟重放来绕过。玩家底栏会明确显示“你的回合”或当前正在等待的角色。
- GM 骰子默认只显示在主控台。勾选「广播给玩家」后，该次结果才会出现在玩家页。
- 「共享公告 / 手册」会同步到所有玩家页；玩家的「我的笔记」只保存在自己的浏览器。

## 常见问题

- 玩家打不开：确认主机服务正在运行、地址是主机的局域网/Radmin IP、端口为 `8090`，并允许 Python 通过防火墙。
- 玩家看到旧界面：刷新一次页面；HTML 页面设置为不缓存，地图和头像资源则使用内容哈希缓存。
- 地图首次加载慢：检查地图图片尺寸；图片会在首次访问后由浏览器缓存。
- 主机电脑休眠：服务器会停止响应，保持电脑唤醒即可。
- 名字无法加入：通常是同名玩家仍被服务器认为在线；换一个昵称，或等待约 35 秒后重新加入。
