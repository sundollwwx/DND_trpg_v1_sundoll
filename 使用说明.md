# 桑哆尔跑团台

这是一个给熟人小团使用的本机 Web 跑团桌面：主机在自己的电脑上开服务，朋友用浏览器进入玩家页，无需安装软件或注册账号。

棋子是纯地图单位，只保留当前 HP、最大 HP 和 AC；不包含职业、法术、属性、技能、装备、状态或先攻规则。

## 主要功能

- 地图、多地图切换、网格与吸附
- 棋子放置、移动、骑乘、所属玩家和棋子库
- 棋子 HP / 最大 HP / AC
- 迷雾、协作涂鸦、普通骰子、BGM、常用链接
- 战役保存、导入导出、项目文件夹自动写盘
- 主机推送的玩家联机

## 快速开团

1. 在项目根目录双击「启动桑哆尔」（macOS 为 `.command`，Windows 为 `.bat`）；或运行：

   ```bash
   python3 主控台/联机服务器.py
   ```

2. 主机浏览器打开 `http://localhost:8090/主控台/主控台.html`，在「📡 联机」中点击「开启玩家模式」。

3. 把「复制玩家观看地址」得到的链接发给朋友，例如：

   ```text
   http://192.168.x.x:8090/主控台/观战.html?v=24
   ```

同一 Wi-Fi 下直接使用本机局域网 IP 即可。若通过 Radmin VPN 联机，朋友使用 Radmin 分配的虚拟网卡 IP；双方都需要允许该端口通过系统防火墙。

## 为什么这版联机更顺

- 只同步当前正在使用的一张地图，不再把全部楼层和棋子库一起发送。
- 地图和头像首次传到服务器后会变成可缓存的资源地址，不再在每条 SSE 消息中携带 Base64 大图片。
- 只有实际改动时才推送完整状态；玩家移动、HP 和 AC 修改是很小的动作包，不会再紧接着回传一份大快照。

因此 Radmin 主要传的是地图操作和小型状态变化，通常比旧版稳定得多。第一次进入地图仍要下载一次地图图片；这属于正常加载，之后浏览器会复用缓存。

## 玩家权限

主机在右侧选中棋子后，填写「所属玩家」。玩家页输入同样的名字，即可操作自己的棋子：

- 拖动移动
- 修改当前 HP、最大 HP、AC
- 使用骰子和协作涂鸦

其他棋子只有查看权限。

## 保存与旧数据迁移

主控台和棋子库加载旧存档时，会自动丢弃旧规则字段，只留下棋子运行需要的轻量数据。绑定项目文件夹后，当前状态会写到：

```text
主控台/状态/当前状态.json
主控台/状态/战役/<战役名>/存档.json
```

旧备份文件没有被删除；如需保留旧版规则资料，可继续把它们当作历史备份。

## 常见问题

- 玩家打不开：确认主机服务正在运行、地址是主机的局域网/Radmin IP、端口为 `8090`，并允许 Python 通过防火墙。
- 玩家看到旧界面：刷新一次页面；新版链接带有 `v=24`，避免浏览器继续使用旧缓存。
- 地图首次加载慢：检查地图图片尺寸；图片会在首次访问后由浏览器缓存。
- 主机电脑休眠：服务器会停止响应，保持电脑唤醒即可。
