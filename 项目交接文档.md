# 桑哆尔跑团台项目交接文档

更新日期：2026-09-02

## 1. 项目定位

这是一个给熟人小团使用的本机 Web 跑团台。GM 在自己的 Windows 或 macOS 电脑上启动 Python 服务，玩家只需打开浏览器，不需要安装客户端。

当前版本已经可以完成地图展示、棋子操作、先攻与时间管理、玩家联机、骰子、公告、BGM 和文件夹存档等核心流程，但仍按“朋友之间使用”的规模设计，不是面向陌生用户或商业部署的完整平台。

项目有三条重要边界：

- 不实现完整 D&D 规则引擎。棋子规则属性只保留当前 HP、最大 HP 和 AC；状态只做记录、倒计时和展示，不自动修改数值或判定规则。
- 主控台是 GM 权威端。服务器只保存当前联机状态，正式战役和棋子库仍由主控台写入根目录的 `存档/`。
- 玩家身份是房间码加临时会话令牌，适合熟人小团；尚未建设账号、密码、权限后台和公网多房间系统。

## 2. 快速启动与关键入口

### 主机启动

- macOS：双击 `启动桑哆尔.command`。
- Windows：双击 `启动桑哆尔.bat`。
- 命令行：`python3 start_server.py`。
- 自定义端口：`python3 start_server.py --port 8092`。

默认端口为 `8090`。启动器会等待 `/api/health` 可用，再打开主控台。

### 页面入口

- GM 主控台：`主控台/主控台.html`
- 玩家页：`主控台/玩家.html`
- 地图工坊：`asset/地图/地图工坊.html`
- 棋子库管理页：`asset/棋子库/棋子库.html`

局域网玩家地址格式：

```text
http://<主机局域网IP>:8090/主控台/玩家.html?room=<房间码>
```

服务器实现位于 `主控台/联机服务器.py`；根目录的 `start_server.py` 是便于 Windows 和命令行调用的英文入口。

## 3. 当前目录与数据职责

```text
桑哆尔的世界/
├── asset/
│   ├── 地图/                 地图工坊、共享渲染器、材质和战役地图
│   ├── 棋子库/               棋子库页面、默认模板和当前立绘
│   └── 音乐/                 示例 BGM
├── 主控台/                   GM 页面、玩家页面、样式、逻辑和联机服务器
├── 存档/                     唯一正式存档根目录，不提交 Git
├── 备团/                     剧情、速查卡和备团模板，不是程序备份
├── start_server.py           跨平台服务器入口
├── 启动桑哆尔.command/.bat   本机启动入口
├── 启动 Tunnel.command/.bat  可选外网 Tunnel 入口
├── 使用说明.md               面向使用者的操作说明
└── 项目交接文档.md           本文档
```

补充说明：

- `.sundoll-cache/联机资源/` 是服务器生成的地图、头像联机缓存，可删除后重建，不是存档。
- `主控台/状态/当前状态.json` 是旧版状态文件，当前正式存档不再依赖它。暂时只作为旧数据迁移兼容项保留；确认所有旧数据均迁移后可单独清理。
- `备团/` 是跑团内容制作区，其中包含模板和“烬鳞讨伐”备团资料，不能按“备份文件夹”删除。
- `.DS_Store`、`.idea/` 和 `.tools/` 都不是运行依赖，已由 `.gitignore` 忽略；后续可做一次非功能性清理。

## 4. 模块状态总览

| 模块 | 当前状态 | 下一步重点 |
| --- | --- | --- |
| 主控台 UI | 第一轮重构已完成 | 小屏布局、细节一致性和真实开团流程打磨 |
| 玩家页 | 核心参与功能已完成 | 移动端专项、更多反馈和多玩家长期联调 |
| 联机服务器 | SSE、动作包、权限和缓存已完成首轮 | 弱网、长时间重连、真实多机和公网验证 |
| 先攻与世界时间 | 已可用于开团 | 补齐玩家结束回合时的状态倒计时一致性测试 |
| 棋子状态与可见性 | 已完成首轮 | 逐玩家可见性、视线遮挡和更细的敌我信息策略 |
| 地图工坊与材质 | 六类双样式和显式选择已完成 | 新素材、覆盖层、物件层和量化性能测试 |
| 棋子库与立绘 | 结构已收敛，引用完整 | 继续补图并做编辑/同步回归 |
| 存档 | 已统一为绑定 `存档/` 文件夹 | 跨浏览器恢复测试、错误提示和迁移收尾 |
| 跨平台启动 | 脚本已具备 | Windows 实机和真实 Cloudflare Tunnel 验证 |
| 自动化验证 | 仅有人工和语法检查 | 建立可重复的 API、存档和浏览器回归测试 |

## 5. 主控台 UI

### 已经完成

- 顶部只保留联机、地图、存档、地图切换、视图开关和保存状态等高频入口；说明性大段文字已移除。
- 左侧工作区分为“单位、地图、绘制、资源”四类，可整体隐藏。
- 旧的临时地图编辑功能已从主控台左栏移除；正式地块制作统一放在地图工坊，主控台只保留开团时需要的地图、迷雾和战术涂鸦操作。
- 右侧详情栏按当前选中棋子展示信息和编辑入口。
- 地图快捷浮窗默认位于左上角，可拖动；位置保存在浏览器本地，下次打开会恢复。
- 地图下方已有可折叠“游戏流程”条，先攻与时间共用相同展开高度。
- 已统一大部分暗色面板、暖金强调、按钮、输入框、Toast、Modal 和禁用/高亮状态。

### 还需要完成

- 对 900px 以下窗口和手机横竖屏做专项布局，不只依赖当前媒体查询。
- 用一次完整开团流程检查键盘焦点、触控、Tooltip、弹窗层级和误触问题。
- 继续收敛历史 CSS；`主控台/style.css` 体积较大，仍有重复样式和旧选择器可整理。
- 统一主控台和玩家页的共享视觉组件。目前玩家页仍以内嵌 CSS/JS 为主，维护时容易产生样式漂移。

### 关键文件

- `主控台/主控台.html`
- `主控台/style.css`
- `主控台/app.js`

## 6. 玩家页

### 已经完成

- 原 `主控台/观战.html` 已改名为 `主控台/玩家.html`，启动说明和分享链接均已切换到新名称。
- 玩家通过名字、房间码和服务器签发的临时会话令牌加入；刷新页面可恢复自己的会话。
- 可查看当前地图、公开棋子、先攻、世界时间、共享网站、共享公告和公开骰点。
- 玩家可以进行准备/暂离、测距、地图指示、协作涂鸦、掷骰、个人笔记、休息请求和结束回合。
- 玩家可以拖动自己名下且当前允许行动的棋子，并修改自己棋子的 HP、最大 HP 和 AC。
- 玩家点击友好棋子可查看公开 HP/AC、状态和备注；敌方和中立单位不会获得同等级数值公开。
- 回合制时显示当前行动棋子的本回合路径；换回合后路径清空。
- 玩家页使用与主控台一致的地图材质资源、BGM 广播和公共状态。

### 还需要完成

- 真实手机和平板上的触控拖动、缩放、测距、涂鸦和底栏空间仍需专项验证。
- 需要至少两台玩家电脑同时联调：同名加入、准备状态、同时拖动、断线重连、切图和回合切换。
- 可以继续增加不改变 GM 权威的参与方式，例如场景投票、线索收藏、角色目标或玩家可见标记；加入前需先确定玩法边界。
- 若未来允许陌生人加入，需要升级为真正的账号或邀请令牌体系，并加入房间管理、踢出、重新授权和审计记录。

### 关键文件

- `主控台/玩家.html`
- `主控台/联机服务器.py`

## 7. 联机服务器与同步

### 已经完成

- 服务器使用 Python 标准库实现，无第三方 Python 依赖，默认监听 `0.0.0.0:8090`。
- 主控台通过 `/api/state` 推送权威公共快照；玩家通过 `/api/events` 的 SSE 接收状态和动作。
- 只同步当前地图，不再把全部楼层和棋子库放进每次推送。
- 地图和头像从 Base64 提取为内容哈希资源，并缓存到 `.sundoll-cache/联机资源/`，浏览器可以复用缓存。
- 玩家移动、HP/AC 修改、涂鸦和回合结束采用小型动作包，不在每次动作后立即回传整张大状态。
- 动作带版本号和 `actionId`；服务器具备动作幂等、历史补发、回合版本检查、请求大小限制和基础限流。
- `/api/state` 与 GM 专用动作限制为本机请求；玩家动作必须携带有效临时会话。
- 已验证无令牌动作返回 401、越回合移动返回 403、重复动作不重复执行、限流返回 429。
- 空闲连接只发送小型 SSE 心跳；重连后可通过事件编号和公共快照恢复。

### 还需要完成

- 在两台以上真实电脑上进行 2–4 小时连续开团测试，覆盖睡眠唤醒、Wi-Fi 切换、短暂断网和浏览器刷新。
- 增加可重复的弱网测试，记录延迟、丢包、重连时间、动作重复率和快照大小。
- 验证长时间运行后 `RECENT_ACTIONS`、会话、限流记录和资源缓存的内存/磁盘变化。
- 当前服务器状态保存在内存中，服务器重启后需要主控台重新加载存档并推送；如未来需要无人值守服务器，再设计服务端持久化，但不要与现有正式存档重复维护。
- 当前模型是单房间、单 GM；多房间和公网托管不在现有架构范围内。

### 关键文件

- `主控台/联机服务器.py`
- `start_server.py`
- `主控台/app.js` 中的联机状态构建、SSE 合并与动作处理部分

## 8. 先攻、世界时间和回合权限

### 已经完成

- 主控台和玩家页都使用可折叠的游戏流程条，并可在“先攻 / 时间”之间切换。
- 支持自由模式和回合制；自由模式允许玩家移动自己名下棋子，回合制只允许当前先攻角色的所属玩家移动。
- GM 在任何模式下都可操作任意棋子。
- 先攻条目可关联棋子，使用稳定条目 ID 和 `turnSerial` 防止换回合期间的过期拖动。
- 玩家拖动在松开时一次提交；服务器拒绝后客户端回滚，不按鼠标移动逐帧发网络请求。
- 本回合路径会在主控台和所有玩家页同步；下一位、手动换人、重置、删除当前单位或退出回合制时清空。
- 世界时间显示年、周、日和 `HH:MM:SS`；固定采用 52 周/年、7 天/周、24 小时/天。
- 短休推进 1 小时，长休推进 8 小时。
- 每个完整战斗轮推进 6 秒，不是每个角色行动推进 6 秒。
- 有剩余回合数的状态在 GM 点击“下一位”离开该单位回合时递减，到 0 自动移除。

### 还需要完成

- 重点回归“玩家点击结束回合”的状态倒计时：当前 GM 本地“下一位”会递减状态，但玩家结束回合走服务器动作链，尚需补齐并验证两条路径行为完全一致。
- 继续验证骑手/坐骑、删除当前棋子、空先攻条目、重新排序和断线期间换回合等边界。
- 若以后允许不同战斗采用不同轮时长，可保留 `secondsPerRound` 配置；当前 UI 仍应默认固定为 6 秒。

### 关键文件

- `主控台/app.js` 中的 `encounter`、世界时间、路径和推进逻辑
- `主控台/玩家.html` 中的只读流程条与玩家拖动逻辑
- `主控台/联机服务器.py` 中的 `can_move_token()`、`apply_action()` 和 `endTurn`

## 9. 棋子数据、状态、隐藏和迷雾

### 已经完成

- 棋子规则属性已收敛为当前 HP、最大 HP 和 AC，不再保存职业、法术、属性、技能、装备等自动规则数据。
- 右侧详情栏支持内置状态和自定义状态、剩余自身回合数、玩家可见/仅 GM 可见以及移除。
- 支持玩家公开备注和 GM 私密备注；构建公共状态时会过滤私密内容。
- 支持 `hiddenFromPlayers`：GM 页面仍显示并可操作，玩家页完全不接收该棋子。
- 与隐藏棋子相关的先攻当前项和移动路径也会从公共状态中过滤。
- 主控台迷雾接近不透明，玩家迷雾完全不透明，并覆盖棋子、路径和涂鸦，避免看到背后轮廓。
- 完全处于迷雾内的棋子不会进入玩家公共状态。

### 还需要完成

- 当前迷雾是全体玩家共享，不是“每个玩家各自探索”的逐玩家迷雾。
- 尚未实现基于墙体和障碍的视线遮挡、动态照明或自动探索。
- 状态不自动影响移动、先攻、HP 或 AC，这是刻意保留的轻规则设计；若要改变必须先重新确认产品边界。
- 需要继续审查隐藏棋子与骑乘、跨地图移动、先攻删除和断线动作历史组合时是否仍无信息泄漏。

## 10. 地图工坊与地图材质

### 已经完成

- 地图工坊与主控台共用 `asset/地图/tile-renderer.js`，避免两边材质显示逻辑分叉。
- 已有六类 AI 静态材质，每类两个可直接选择的样式：石板、木地板、草地、土路、水面、石墙。
- 地块样式不再随机决定。每格通过 `cellVariants` 明确记录样式编号；旧地图没有该字段时统一回退到样式 I。
- 墙边、水岸和草土交界仍由 Canvas 根据相邻格绘制基础边缘。
- 地图工坊已支持搜索、收藏、最近使用、多个笔刷尺寸、拖拽绘制、Shift 直线、直线、矩形、填充、选区、撤销和重做。
- 复制、移动、旋转、草稿、模板、JSON 导入导出和主控台导入都会保留 `cellVariants`。
- 已在真实浏览器验证“石板·样式 II”的放置、拾取和页面重载。
- 已开放 100×100 地图尺寸。

### 还需要完成

- 100×100 地图只完成页面级回归，尚未量化首次绘制、拖动、缩放、缓存命中、内存和长时间编辑性能。
- 完整 autotile/bitmask 尚未实现；当前只有基础边缘，不包含所有墙角、道路交叉、尽头和复杂连接组合。
- 覆盖层和透明物件层尚未实现。血迹、污渍、落叶、桌椅、门、树木等不应继续扩展为基础地块。
- 下一批基础素材建议按顺序制作：
  1. 沙地、冰面、岩浆；
  2. 木墙、岩石、碎石；
  3. 再进入透明家具、环境物件和大型物件。
- 素材需求和分类规则保存在 `asset/地图/待补充材质清单.md`；该文件仍有效，不应删除。

### 关键文件

- `asset/地图/地图工坊.html`
- `asset/地图/tile-renderer.js`
- `asset/地图/材质/`
- `asset/地图/待补充材质清单.md`
- `主控台/app.js` 中的地图导入与材质重绘逻辑

## 11. 棋子库与立绘

### 已经完成

- 棋子库正式数据统一写入 `存档/棋子库/棋子库.json`。
- `asset/棋子库/默认预设.js` 只用于首次启动提供默认模板，不是第二份正式存档。
- 当前正式棋子库有 69 条预设，69 条立绘路径均已检查有效。
- `asset/棋子库/立绘/角色/` 已永久删除；保留 `asset/棋子库/立绘/玩家/`，其中当前有 4 张玩家立绘。
- 旧立绘归档和重复角色图已清理；当前立绘按分类存放在 `asset/棋子库/立绘/`。
- 主控台、棋子库页面和玩家页均改用新的 `asset/棋子库/` 路径。
- 立绘导入、命名、归档和高清缓存规则记录在 `asset/棋子库/立绘制作流程.md`。

### 还需要完成

- 继续补充缺少立绘或需要更换的棋子，并在每次批量调整后重新检查全部 `iconImgPath`。
- 在 Chrome/Edge 中回归“绑定棋子库文件夹 → 导入成图 → 主控台同步 → 玩家页显示”的完整流程。
- 棋子库页面、主控台内置编辑器和正式 JSON 目前有多个操作入口；后续可以决定是否保留主控台轻量编辑，仅把批量立绘管理留给棋子库页面。
- 不要重新建立 `角色/` 目录；玩家角色继续归档在 `立绘/玩家/`。

## 12. 存档系统

### 已经完成

- 唯一正式存档根目录为项目根目录下的 `存档/`。
- 主控台存档菜单只保留“存档”和“读取存档”两个用户操作。
- 第一次使用时通过 File System Access API 绑定整个 `存档/` 文件夹。
- 战役保存到 `存档/战役/<战役ID>-<战役名>/当前存档.json`。
- 手动保存和读档前生成恢复版本，自动备份目录最多保留最近 10 份常规备份。
- 全局棋子库独立保存在 `存档/棋子库/棋子库.json`，不再复制到每个战役。
- `存档/存档索引.json` 维护战役列表；当前索引中有两个战役，JSON 可正常解析。
- 浏览器缓存只作为意外关闭的恢复点；未绑定文件夹时会明确降级为“仅浏览器缓存”。
- 旧 IndexedDB 战役和旧格式字段仍保留一次性迁移逻辑。

### 还需要完成

- 在新浏览器配置文件中完整验证首次授权、拒绝授权、权限失效、重新绑定、另存恢复和跨电脑复制。
- File System Access API 主要依赖 Chrome/Edge；Safari/Firefox 主机端的正式存档能力需要明确提示或另做兼容方案。
- 审查旧 IndexedDB 和 `主控台/状态/当前状态.json` 的真实迁移使用率，确认不再需要后再删除兼容代码和旧文件。
- 为磁盘空间不足、文件被占用、JSON 损坏和目录改名补充更清楚的恢复指引。
- `存档/` 已被 `.gitignore` 忽略；不要为了上传项目而强行加入 Git。

### 正式结构

```text
存档/
├── 存档索引.json
├── 棋子库/
│   └── 棋子库.json
└── 战役/
    └── <战役ID>-<战役名>/
        ├── 当前存档.json
        └── 自动备份/
```

## 13. BGM、骰子、公告与共享资源

### 已经完成

- BGM 可绑定音乐文件夹或临时选择文件，支持播放、暂停、切歌、音量和循环。
- 主控台可通过 GM 专用接口广播 BGM 状态，玩家页从服务器同源地址播放。
- GM 骰子默认私密；勾选广播后才把本次结果发给玩家。
- 玩家骰点会同步到主控台和其他玩家页，并通过动作 ID 避免网络重试重复显示。
- 共享公告/手册和常用网站进入公共战役状态；链接只接受 `http`/`https`。
- 玩家个人笔记只存在自己的浏览器，不会同步给 GM 或其他玩家。

### 还需要完成

- 在 Safari、Chrome、Edge 上验证浏览器自动播放限制和断线后 BGM 时间恢复。
- 大型音频文件、歌曲切换和 Tunnel 下的首次加载需要测量带宽与等待时间。
- 如以后需要长期线索库，应与当前 4000 字轻量公告分开设计，不要直接把公告框扩展成复杂文档系统。

## 14. Windows、macOS、Radmin 与 Tunnel

### 已经完成

- macOS 和 Windows 都有本机启动脚本，并统一调用 `start_server.py`。
- 启动器会先检查健康接口，再打开主控台，避免浏览器先于服务器启动。
- 同一 Wi-Fi 和 Radmin 虚拟局域网均可通过主机 IP 加端口访问。
- 已提供 Cloudflare Quick Tunnel 脚本；找不到 `cloudflared` 时会给出安装提示。
- 主控台支持配置自定义服务器地址，兼容自定义端口和 HTTPS Tunnel 同源访问。

### 还需要完成

- Windows 主机尚未实机完成启动、Python 检测、防火墙、中文路径和浏览器联机全流程验证。
- 尚未用真实 Cloudflare Quick Tunnel 完成异地网络验证。
- macOS 与 Windows 间的存档文件夹复制、恢复和路径编码仍需实测。
- 需要测试主机休眠、系统防火墙弹窗、端口占用和 `cloudflared` 中途退出时的提示与恢复。
- Quick Tunnel 地址每次可能变化；固定域名需要另行配置 Named Tunnel，但不属于当前必需功能。

## 15. 已完成的验证

目前已有的验证证据主要来自人工浏览器联调和语法检查：

- 主控台与玩家页真实 SSE 链路：模式、轮次、时间、先攻、公开状态、共享网站和隐藏棋子同步。
- GM 私密备注未出现在玩家页面；玩家迷雾完全不透明。
- 回合权限：无令牌、越权、越回合和过期拖动请求被拒绝。
- 玩家合法移动、路径同步、换回合清理、重复 `actionId` 幂等和基础限流。
- 地图“石板·样式 II”放置、拾取和重载，`cellVariants` 可持久化。
- 正式棋子库 JSON 可解析；69 条立绘引用均有效；玩家立绘 4 张；旧 `角色/` 目录不存在。
- `存档索引.json` 与 `棋子库.json` 均可正常解析。
- JavaScript/Python 曾完成语法检查，当前仓库 `git diff --check` 通过。

当前没有自动化测试文件。后续不能把“语法通过”或“页面能打开”等同于 Windows、弱网或长时间开团已经验证。

## 16. 建议后续优先级

### P0：先保证可以稳定开团

1. 修正并验证玩家“结束回合”与 GM“下一位”的状态倒计时一致性。
2. 用两台以上真实电脑完成完整战斗回归，包括断线、刷新、切图、骑乘和隐藏棋子。
3. 在 Windows 主机完成启动、防火墙、中文路径、存档和玩家加入实测。

### P1：联机与性能

1. 做 100×100 地图量化测试，记录首绘时间、交互帧率、内存和缓存命中。
2. 做弱网与长时间断线恢复测试，记录快照大小、重连时间和重复动作。
3. 用真实 Cloudflare Quick Tunnel 完成一次异地联机。

### P2：地图内容与编辑体验

1. 按清单制作沙地、冰面、岩浆，再制作木墙、岩石和碎石。
2. 设计透明覆盖层和物件层的数据格式，再生成家具、门、树木等素材。
3. 完成更完整的 autotile/bitmask，并避免破坏旧地图兼容。

### P3：长期维护

1. 把玩家页的共享样式和公共逻辑拆出，减少与主控台重复。
2. 建立 API 权限、存档迁移和浏览器主流程的自动化回归。
3. 确认旧状态文件和 IndexedDB 迁移不再需要后，再删除兼容代码。

## 17. Git 交接注意事项

- 当前分支是 `main`，远端为 `origin/main`。
- 工作区包含大量尚未提交的资源迁移和功能重构：旧的 `地图/`、`棋子库/`、`音乐/` 路径显示为删除，新的 `asset/` 显示为新增，这是同一次目录重构的两面。
- 不要执行 `git reset --hard` 或 `git checkout --`，否则会丢失当前重构。
- 后续提交时应整体暂存资源迁移，让 Git 自动识别 rename；不要只提交旧路径删除或只提交 `asset/` 新文件。
- `存档/`、`.sundoll-cache/`、IDE 设置和系统缓存均已忽略，不应上传。
- 建议在提交前依次执行：

```bash
git diff --check
python3 -m py_compile start_server.py 主控台/联机服务器.py
git status --short
```

再进行一次主控台、玩家页、地图工坊和棋子库的人工浏览器回归。

## 18. 交接原则

- 修改前先确认是“正式存档”“静态资源”还是“可再生缓存”，不要重新制造第二套存档路径。
- 主控台完整状态和 GM 私密信息绝不能直接发送给玩家；所有公共字段必须经过白名单构建。
- 玩家前端的禁用样式不是权限边界，关键动作必须继续在服务器端校验。
- 新地图素材优先做可复用的无缝底材或透明物件，不为每个格子运行时生成 AI 图片。
- 保持旧地图和旧战役可读取；新增字段都要有默认值和归一化逻辑。
- 每次宣称跨平台、弱网或大地图完成前，都应留下对应真实环境或量化测试证据。
